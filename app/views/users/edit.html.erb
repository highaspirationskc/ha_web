<div class="max-w-7xl mx-auto py-6 sm:px-6 lg:px-8">
  <div class="px-4 py-6 sm:px-0">
    <nav class="flex mb-6" aria-label="Breadcrumb">
      <ol class="inline-flex items-center space-x-1 md:space-x-3">
        <li class="inline-flex items-center">
          <%= link_to users_path, class: "inline-flex items-center text-sm font-medium text-gray-700 hover:text-indigo-600" do %>
            Users
          <% end %>
        </li>
        <li>
          <div class="flex items-center">
            <svg class="w-3 h-3 text-gray-400 mx-1" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 9 4-4-4-4"/>
            </svg>
            <%= link_to @user.email, user_path(@user), class: "ml-1 text-sm font-medium text-gray-700 hover:text-indigo-600 md:ml-2" %>
          </div>
        </li>
        <li aria-current="page">
          <div class="flex items-center">
            <svg class="w-3 h-3 text-gray-400 mx-1" aria-hidden="true" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 6 10">
              <path stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="m1 9 4-4-4-4"/>
            </svg>
            <span class="ml-1 text-sm font-medium text-gray-500 md:ml-2">Edit</span>
          </div>
        </li>
      </ol>
    </nav>

    <%= form_with model: @user, url: user_path(@user), method: :patch, class: "space-y-6" do |form| %>
      <% if @user.errors.any? %>
        <div class="rounded-md bg-red-50 p-4">
          <h3 class="text-sm font-medium text-red-800">
            <%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:
          </h3>
          <ul class="list-disc list-inside text-sm text-red-700 mt-2">
            <% @user.errors.full_messages.each do |message| %>
              <li><%= message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <% if @can_edit_profile || @can_edit_password %>
        <div class="bg-white shadow px-4 py-5 sm:rounded-lg sm:p-6">
          <div class="md:grid md:grid-cols-3 md:gap-6">
            <div class="md:col-span-1">
              <h3 class="text-lg font-medium leading-6 text-gray-900">User Information</h3>
              <p class="mt-1 text-sm text-gray-500">
                <% if @can_edit_profile %>
                  Update user details.
                <% else %>
                  Update your password.
                <% end %>
              </p>
            </div>
            <div class="mt-5 md:mt-0 md:col-span-2">
              <div class="grid grid-cols-6 gap-6">
                <% if @can_edit_profile %>
                  <div class="col-span-6 sm:col-span-3">
                    <%= form.label :first_name, "First Name", class: "block text-sm font-medium text-gray-700" %>
                    <%= form.text_field :first_name, class: "mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md" %>
                  </div>

                  <div class="col-span-6 sm:col-span-3">
                    <%= form.label :last_name, "Last Name", class: "block text-sm font-medium text-gray-700" %>
                    <%= form.text_field :last_name, class: "mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md" %>
                  </div>

                  <div class="col-span-6 sm:col-span-4">
                    <%= form.label :email, class: "block text-sm font-medium text-gray-700" %>
                    <%= form.email_field :email, class: "mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md" %>
                  </div>
                <% end %>

                <% if @can_edit_password %>
                  <div class="col-span-6 sm:col-span-3">
                    <%= form.label :password, "New Password", class: "block text-sm font-medium text-gray-700" %>
                    <%= form.password_field :password, class: "mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md" %>
                    <p class="mt-1 text-xs text-gray-500">Leave blank to keep current password</p>
                  </div>

                  <div class="col-span-6 sm:col-span-3">
                    <%= form.label :password_confirmation, "Password Confirmation", class: "block text-sm font-medium text-gray-700" %>
                    <%= form.password_field :password_confirmation, class: "mt-1 focus:ring-indigo-500 focus:border-indigo-500 block w-full shadow-sm sm:text-sm border-gray-300 rounded-md" %>
                  </div>
                <% end %>

                <% if @can_change_status %>
                  <div class="col-span-6 sm:col-span-3">
                    <label class="block text-sm font-medium text-gray-700">Status</label>
                    <div class="mt-2">
                      <%= form.check_box :active, class: "focus:ring-indigo-500 h-4 w-4 text-indigo-600 border-gray-300 rounded" %>
                      <%= form.label :active, "Active", class: "ml-2 text-sm text-gray-900" %>
                    </div>
                  </div>
                <% end %>
              </div>
            </div>
          </div>
        </div>

        <% if @user.mentee.present? && @can_edit_profile %>
          <div class="bg-white shadow px-4 py-5 sm:rounded-lg sm:p-6">
            <div class="md:grid md:grid-cols-3 md:gap-6">
              <div class="md:col-span-1">
                <h3 class="text-lg font-medium leading-6 text-gray-900">Mentee Information</h3>
                <p class="mt-1 text-sm text-gray-500">Assign a team and mentor for this mentee.</p>
              </div>
              <div class="mt-5 md:mt-0 md:col-span-2">
                <div class="grid grid-cols-6 gap-6">
                  <div class="col-span-6 sm:col-span-3">
                    <%= render "admin/shared/combobox",
                        name: "mentee[team_id]",
                        label: "Team",
                        options: @teams.map { |t| [t.name, t.id] },
                        selected_value: @user.mentee.team_id,
                        selected_text: @user.mentee.team&.name,
                        placeholder: "Search teams..." %>
                  </div>

                  <div class="col-span-6 sm:col-span-3">
                    <%= render "admin/shared/combobox",
                        name: "mentee[mentor_id]",
                        label: "Mentor",
                        options: @mentors.map { |m| ["#{m.user.first_name} #{m.user.last_name} (#{m.user.email})", m.id] },
                        selected_value: @user.mentee.mentor_id,
                        selected_text: @user.mentee.mentor ? "#{@user.mentee.mentor.user.first_name} #{@user.mentee.mentor.user.last_name} (#{@user.mentee.mentor.user.email})" : nil,
                        placeholder: "Search mentors..." %>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>

        <div class="flex justify-end space-x-3">
          <%= link_to "Cancel", user_path(@user), class: "inline-flex justify-center py-2 px-4 border border-gray-300 shadow-sm text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50" %>
          <% if @can_delete %>
            <%= button_to "Delete User", user_path(@user), method: :delete, class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700", data: { confirm: "Are you sure?" } %>
          <% end %>
          <%= form.submit "Save Changes", class: "inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-indigo-600 hover:bg-indigo-700" %>
        </div>
      <% end %>
    <% end %>
  </div>
</div>

# Dependabot config - weekly grouped updates targeting the dev branch
# - Patches and minors grouped
# - Major versions ignored by default
# - PRs open against dev (promote to main via PR when ready for prod)
# - Last updated: 2026-08-09

version: 2
updates:
  - package-ecosystem: "bundler"
    directory: "/"
    target-branch: "dev"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5
    groups:
      rails:
        patterns:
          - "rails"
          - "action*"
      production:
        dependency-type: "production"
        exclude-patterns:
          - "rails"
      development:
        dependency-type: "development"
    ignore:
      - dependency-name: "*"
        update-types: ["version-update:semver-major"]

  - package-ecosystem: "github-actions"
    directory: "/"
    target-branch: "dev"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 3

  - package-ecosystem: "docker"
    directory: "/"
    target-branch: "dev"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 2
